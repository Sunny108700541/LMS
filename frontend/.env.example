# Base URL of the LMS API, including the version prefix.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1

# Name of the access-token cookie the API sets. The Next.js middleware checks for
# its presence to decide whether to render a protected route or redirect to sign-in.
NEXT_PUBLIC_ACCESS_COOKIE=lms_access_token
